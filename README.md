# Notas:
Primer programa en node
```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, -a         Es la base de la tabla de multiplicar       [number] [required]
  -l, --listar   Es para listar la tabla de multiplicar
                                           [boolean] [required] [default: false]
  -h, --limite   Es para poner limite a la tabla
                                               [number] [required] [default: 10]
```